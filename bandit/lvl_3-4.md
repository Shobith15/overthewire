Bandit Level 3-4 

Here we use basic "ls", "cat" + "cd" (change directory)

So directories in linux are highlighted in colours. It depends on the distro and the purpose of 
the directory. 

So to change to another directory we use cd (to change directory). Then use cat to read files in it.


